import addition # we import the files to the tested and also the pytest library
import pytest
#we write test for the individual functions in the addition file
def test_add():
    assert True
    assert addition.add(2,3) == 5
  
def test_sub():
    pass
  
